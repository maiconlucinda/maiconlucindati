name: "security.txt expiry reminder"

on:
  schedule:
    - cron: "0 9 1 7 *" # Runs July 1st every year
  workflow_dispatch:

permissions:
  issues: write

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Open reminder issue
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "Reminder: renew security.txt before July 2027",
              body: "The `/.well-known/security.txt` file expires on **2027-07-23**.\n\nUpdate the `Expires` field and push before that date to stay compliant with RFC 9116.",
              labels: []
            })
